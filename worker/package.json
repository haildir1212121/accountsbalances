{
  "name": "icabbi-webhook-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker — iCabbi completed-trip webhook with Supabase",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
